import 'strings.dart';

/// The translations for English (`en`).
class NotificationsStringsEn extends NotificationsStrings {
  NotificationsStringsEn([String locale = 'en']) : super(locale);

  @override
  String get notifications => 'notifications';
}
