import 'strings.dart';

/// The translations for Portuguese (`pt`).
class NotificationsStringsPt extends NotificationsStrings {
  NotificationsStringsPt([String locale = 'pt']) : super(locale);

  @override
  String get notifications => 'notifications';
}
