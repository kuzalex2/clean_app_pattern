import 'strings.dart';

/// The translations for English (`en`).
class InboxStringsEn extends InboxStrings {
  InboxStringsEn([String locale = 'en']) : super(locale);

  @override
  String get inbox => 'inbox';
}
