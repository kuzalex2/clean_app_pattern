import 'strings.dart';

/// The translations for Portuguese (`pt`).
class InboxStringsPt extends InboxStrings {
  InboxStringsPt([String locale = 'pt']) : super(locale);

  @override
  String get inbox => 'inbox';
}
