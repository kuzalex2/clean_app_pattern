import 'strings.dart';

/// The translations for English (`en`).
class MainScreenStringsEn extends MainScreenStrings {
  MainScreenStringsEn([String locale = 'en']) : super(locale);

  @override
  String get main_screen_test => 'Main screen test';
}
